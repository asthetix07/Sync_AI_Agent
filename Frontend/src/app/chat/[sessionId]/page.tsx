// This page exists solely to match the /chat/[sessionId] URL segment.
// ChatLayout is rendered by the shared layout.tsx in the parent directory.
export default function ChatSessionPage() {
  return null;
}
